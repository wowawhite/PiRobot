#import pigpio
#import pigpio_pi

class IODevice():
	def __init__(self):
		# does actually nothing
	def MCP(self):



"""
should be something like this
__init__(self, name = name, dtype = 'Stepper Motors',
                           display = True, GPIO = GPIO,
                           pigpio = pigpio, pigpio_pi = pigpio_pi)
"""